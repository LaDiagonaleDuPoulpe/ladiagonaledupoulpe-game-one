/**
 * Scene key enum
 * List all types of scene, that are loaded in game motor
 */
export enum SceneType {
    main = 'MainScene',
    boot = 'BootScene',
    loading = 'LoadingScene',
    title = 'TitleScene',
    map = 'MapScene'
};